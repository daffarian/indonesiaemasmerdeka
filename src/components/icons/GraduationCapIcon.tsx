import { GraduationCap, ReceiptEuro } from "lucide-react";

export default function GraduationCapIcon({
  className,
}: {
  className?: string;
}) {
  return <GraduationCap className={className} />;
}
